# archive 目录说明

本目录用于本地归档不确定、过时或不适合直接提交的文件。默认通过 `.gitignore` 忽略目录内除本说明外的所有内容。

适合放入这里的内容：

- 代码或目录整理前的状态快照
- 临时备份
- 大型审计 CSV 的归档副本
- 不确定是否还需要的旧工具配置

重要结论应同步写入 `docs/reports/`；`archive/` 只作为本地回滚和审计辅助，不作为 GitHub Pages 运行依赖。
